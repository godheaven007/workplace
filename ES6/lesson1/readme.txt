块级作用域(let, const)

1、ES6为什么要引入块级作用域
2、注意事项：
	 ①、循环体外部是一个作用域，循环体内部是一个单独的作用域
	 ②、不存在变量提升
	 ③、不允许重复申明
3、取代立即执行函数表达式
4、const命令
5、顶层对象window

function Animal(){
	
	this.name = name;
}

class Animal{
	
	puiblic
}